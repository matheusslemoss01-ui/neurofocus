import type { NextConfig } from "next";

const nextConfig: import('next').NextConfig = { typescript: { ignoreBuildErrors: true }, eslint: { ignoreDuringBuilds: true } }; const _nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
